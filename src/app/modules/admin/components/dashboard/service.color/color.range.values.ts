export interface ColorRange {
    successMin: number,
    successMax: number,
    primaryMin: number,
    primaryMax: number,
    warningMin: number,
    warningMax: number
    dangerMin: number,
    dangerMax: number
}